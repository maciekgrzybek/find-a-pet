import React from 'react';

const AddMarker = (props) => {
	const style= {
		width: 50,
		height: 50,
		background: 'red'
	};
	return (
		<div  style={ style }>
		</div>
	);
}

export default AddMarker;